# the dev network speaks .test, never .dev (a real, HSTS-preloaded gTLD)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kingfisher
spec:
  routes:
    - match: Host(`kingfisher.test`)
      kind: Rule
      services:
        - name: kingfisher
          port: 15021
